import React from 'react'

import RedBallLinks from '../index'

function RedBallLinksPreview(props) {
  const {links} = props.value
  return <RedBallLinks links={links} />
}

export default RedBallLinksPreview
